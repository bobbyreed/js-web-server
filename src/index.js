import React from 'react';
import ReactDOM from 'react-dom';

const App = () => {
    return (
        <div>
            <h1>Express server with a bit of React test</h1>
        </div>
    );
};

ReactDOM.render(<App />, document.getElementById('root'));